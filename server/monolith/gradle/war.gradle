apply plugin: "war"

bootWar {
    mainClass = "training.hipster.compose.ServerApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
